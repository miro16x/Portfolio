{
  "name": "skill-setup",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix amirs-portfolio",
    "build": "npm run build --prefix amirs-portfolio",
    "preview": "npm run preview --prefix amirs-portfolio",
    "install:portfolio": "npm install --prefix amirs-portfolio"
  }
}
